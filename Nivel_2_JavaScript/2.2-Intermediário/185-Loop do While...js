// do while = primeiro executa o bloco de código 
//e depois verifica a condição, ou seja, 
//o bloco de código é executado pelo menos uma vez

let contador = 0;

do {
    console.log(`Valor do contador: ${contador}`);
    contador++;
} while (contador < 11);