// For of = percorre cada elemento de um array ou string

// For of com array
let frutas = ['Maçã', 'Pêra', 'Uva'];

for (let fruta of frutas) {
    console.log(fruta);
}

// For of com string
let nome = 'Matheus';

for (let letra of nome) {
    console.log(letra.toWellFormed());
}